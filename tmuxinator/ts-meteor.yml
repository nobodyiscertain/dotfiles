# ~/.tmuxinator/ts-meteor.yml
# you can make as many tabs as you wish...

project_name: ts-meteor
project_root: ~/Code/thundershell
tabs:
  - bash&logs:
      layout: even-horizontal
      panes:
        - mrt
        - #empty, will just run plain bash
        - compass watch client/
  - vim: "vim ."
  - mongo: mrt mongo
  - logs: #mrt logs
  - database: mrt mongo
